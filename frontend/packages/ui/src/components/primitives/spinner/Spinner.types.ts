/** Props for {@link Spinner}. */
export interface SpinnerProps {
  /** Visual size. Defaults to `md`. */
  size?: 'sm' | 'md' | 'lg';
  /** Accessible label announced via the status role. */
  label?: string;
  /** Extra class name. */
  className?: string;
}
