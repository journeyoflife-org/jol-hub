/** Props for {@link SkipLink}. */
export interface SkipLinkProps {
  /** Target element id (without `#`). Defaults to `main-content`. */
  targetId?: string;
  /** Visible label. Defaults to Lithuanian + English skip text. */
  label?: string;
}
