/** Props for {@link PageContainer}. */
export interface PageContainerProps {
  children: React.ReactNode;
  /** Narrower measure for long-form text. */
  narrow?: boolean;
  /** Extra class name. */
  className?: string;
}
