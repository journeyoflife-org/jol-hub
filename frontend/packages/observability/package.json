{
  "name": "@jol-hub/observability",
  "version": "1.0.0",
  "private": true,
  "description": "Observability core for the JOL frontend (STEP 16): zero-dependency structured JSON logging with PII redaction, error categorization/fingerprinting/breadcrumbs, client performance batching, and health aggregation. Pure logic — no React/Next imports; safe for server, client and edge.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "tsx --test src/__tests__/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
