# Registry configuration for @journeyoflife-org/* packages
# GitHub Packages requires authentication via NPM_TOKEN (PAT with read:packages, write:packages)

@jol-hub:registry=https://npm.pkg.github.com
