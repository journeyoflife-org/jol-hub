Scaffolding a new vertical spoke: copy `docs/templates/jol-frontend-repo-template/` into a new repo under `journeyoflife-org/jol-site-{vertical}` and run `bash scripts/instantiate.sh {vertical} "{Display Name}"`, then verify zero `__VERTICAL__` / `__VERTICAL_NAME__` placeholders via grep. Publishing shared packages uses the runbook in `docs/architecture/package-publish-runbook.md` with changesets 2.x configured for automated version PRs; registry defaults to GitHub Packages (`https://npm.pkg.github.com`) per D-065.