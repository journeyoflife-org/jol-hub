`scripts/setup-venvs.sh` provisions per-script virtualenvs; `scripts/run_compliance_tests.py --country lt --all` runs GDPR/SOC2/PCI-DSS suites; `scripts/run_tenant_verification.py [--organization-id <uuid>] [--output json]` requires Django settings (`core.settings.development`); `scripts/check-payment-boundary.sh [ROOT]` enforces PCI boundary against the whole checkout; `python scripts/validate_entity_configs.py [-c country] [-t type] [-f json|text] [--base-path .]` validates all `entity.yml` files under `countries/`; `python tools/qoder/qoder.py` exposes `workflow`, `scaffold`, `ropa` subcommands; MCP servers are started by piping JSON-RPC lines into `python tools/mcp-*.py`.